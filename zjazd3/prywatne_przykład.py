class Produkt

    def__init__(self, id, name, price):
        self._id = id
    self.name = name
    self.price = price

    # para metod property służy do odczytu, setter służy do zapisu

    @property
    def full name(self):
    return 'Product: ' + self.name

    @property
    def discount price (self):
        return self.price * 0.8

    @property
    def name(self):
        return self._ = value

    @name.setter
    def name (self, value):
        if len(value) > 3
            self._name = value


    def__init__(self, id, name, price):
        self.price = price
        self._id = id
        self.name = name
        self._discount = 0


    @property
    def discount(self):
        return self._ = discount

    @discount.setter
    def discount(self, value)
        if type(value) == float:
            if 0 <= value <= 0.3:
            self._discount = value

    @discount.deleter
    def discount(self):
        self._discount -= 0


help(Produkt.discount_price)
# kawałek instrukcji dokumentacji

    print(discount())


print(pr1.name)
print(pr1._id)
print(pr1.full_name)
print(pr1.discount_price


pr1 = Produkt(1. 'bulka', 10)

print(pr1.price)
print(pr1.name = 'ala ma kota'
print(pr1.name)
print(pr1._nsme)

print(pr1.name)
print(pr1._id)
print(pr1.full_name)
print(pr1.discount_price



